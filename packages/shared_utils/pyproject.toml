[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "shared-utils"
version = "0.1.5"
description = "Shared utilities for airflow-multi-tenant"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["shared_utils*"]
