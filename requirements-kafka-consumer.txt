# Requirements for Kafka CDC Consumer service
# Lightweight — only what the consumer needs

# FastAPI and web framework (for health endpoints)
fastapi==0.117.0
uvicorn[standard]==0.40.0
pydantic==2.12.5
pydantic-settings==2.12.0

# Database (sync only — no async needed)
sqlalchemy==2.0.48
pymysql==1.1.2
cryptography==46.0.4

# Kafka
kafka-python==2.3.0

# Redis (for message deduplication)
redis==5.3.1

# HTTP (for Airflow API calls)
requests==2.32.5
