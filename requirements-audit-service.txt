# Requirements for Audit Service
# Consumes audit events from Kafka and writes to per-customer MySQL schemas

# FastAPI and web framework
fastapi==0.117.0
uvicorn[standard]==0.40.0
pydantic==2.12.5
pydantic-settings==2.12.0

# Database (sync only — consumer thread is sync)
sqlalchemy==2.0.48
pymysql==1.1.2
cryptography==46.0.4

# Kafka
kafka-python==2.3.0

# Shared utils dependencies
requests==2.32.5
